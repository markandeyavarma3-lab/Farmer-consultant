# Core app
streamlit>=1.40.0

# LLM + Whisper (English STT) via Groq free tier
groq>=0.11.0

# RAG: local vector store + multilingual embeddings
chromadb>=0.5.0
sentence-transformers>=3.0.0

# HTTP for Sarvam STT/TTS
requests>=2.31.0

# Free fallback TTS (no API key needed)
gTTS>=2.5.0

# Local .env loading for development
python-dotenv>=1.0.0
